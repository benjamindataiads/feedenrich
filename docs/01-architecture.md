# Architecture Agentique

## Principe fondamental

**UN agent central** qui reçoit un objectif et décide dynamiquement quels outils utiliser.

```
┌─────────────────────────────────────────────────────────────────┐
│                        ENRICHMENT AGENT                         │
│                                                                 │
│   "Améliore ce produit pour qu'il soit GMC-compliant et        │
│    agent-ready, sans jamais inventer de caractéristique"        │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                   REASONING LOOP                        │   │
│   │                                                         │   │
│   │   1. Observe l'état actuel du produit                   │   │
│   │   2. Identifie les problèmes / opportunités             │   │
│   │   3. Choisit le meilleur outil à appeler                │   │
│   │   4. Exécute et observe le résultat                     │   │
│   │   5. Décide : continuer ou terminer                     │   │
│   │                                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                      TOOLBOX                            │   │
│   │                                                         │   │
│   │  📊 analyze_product      - état initial, gaps, scores   │   │
│   │  🔍 web_search           - chercher faits externes      │   │
│   │  📄 fetch_page           - extraire contenu page        │   │
│   │  👁️ analyze_image        - observer visuels produit     │   │
│   │  ✏️ optimize_field       - améliorer un champ           │   │
│   │  ➕ add_attribute        - ajouter attribut sourcé      │   │
│   │  📏 create_rule          - créer règle de validation    │   │
│   │  ✅ validate_proposal    - vérifier avant commit        │   │
│   │  💾 commit_changes       - appliquer les modifications  │   │
│   │                                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## L'agent décide, pas le code

**Avant (pipeline)** : Le code dit "d'abord cluster, puis audit, puis enrich"

**Maintenant (agentique)** : L'agent observe et décide :
- "Ce produit a un titre trop court → je vais d'abord chercher le nom complet sur le web"
- "L'image montre une couleur claire → je vais analyser l'image pour confirmer"
- "Il manque la matière → je cherche sur le site officiel de la marque"
- "Tout est bon maintenant → je commit"

## Composants techniques

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                │
│  - Upload dataset                                               │
│  - Voir les décisions de l'agent en temps réel                  │
│  - Valider/rejeter les propositions à risque                    │
│  - Exporter le résultat                                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         API (REST)                              │
│  /datasets, /jobs, /products, /agent/run, /proposals            │
└─────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌──────────────────┐ ┌──────────────┐ ┌──────────────────┐
│   PostgreSQL     │ │   Storage    │ │   Queue          │
│  - products      │ │  (S3/GCS)    │ │  (Redis/pg-boss) │
│  - agent_traces  │ │  TSV, images │ │  async jobs      │
│  - proposals     │ │              │ │                  │
└──────────────────┘ └──────────────┘ └──────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      AGENT RUNTIME                              │
│                                                                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │   OpenAI    │    │ Tool        │    │ Trace       │         │
│  │   (GPT-4)   │◄──►│ Executor    │◄──►│ Logger      │         │
│  └─────────────┘    └─────────────┘    └─────────────┘         │
│                              │                                  │
│         ┌────────────────────┼────────────────────┐            │
│         ▼                    ▼                    ▼            │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │ Web Search  │    │ Vision API  │    │ DB/Storage  │         │
│  │ (Serper)    │    │ (OpenAI)    │    │             │         │
│  └─────────────┘    └─────────────┘    └─────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

## Session de l'agent

L'agent maintient un **état de session** pour chaque produit :

```json
{
  "product_id": "SKU123",
  "goal": "Rendre ce produit GMC-compliant et agent-ready",
  "constraints": [
    "Ne jamais inventer de caractéristique non sourcée",
    "Citer toutes les sources",
    "Demander validation humaine si risque > medium"
  ],
  "current_state": {
    "raw_data": { ... },
    "analysis": null,
    "proposals": [],
    "sources_gathered": []
  },
  "trace": [
    { "step": 1, "thought": "...", "tool": "...", "result": "..." }
  ],
  "status": "running"
}
```

## Avantages de cette approche

1. **Adaptabilité** : L'agent s'adapte à chaque produit (simple → peu d'actions, complexe → plus d'investigation)
2. **Explicabilité** : Chaque décision est tracée avec le raisonnement
3. **Évolutivité** : Ajouter un outil = l'agent peut l'utiliser immédiatement
4. **Robustesse** : Si un outil échoue, l'agent peut essayer une autre stratégie
